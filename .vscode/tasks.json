{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Format Proto",
            "type": "shell",
            "command": "npx prettier . --write",
            "options": {
                "cwd": "${workspaceFolder}/packages/proto"
            }
        },
        {
            "label": "Format Server",
            "type": "shell",
            "command": "npx prettier . --write",
            "options": {
                "cwd": "${workspaceFolder}/packages/server"
            }
        },
        {
            "label": "Format App",
            "type": "shell",
            "command": "npx prettier . --write",
            "options": {
                "cwd": "${workspaceFolder}/packages/app"
            }
        },
        {
            "label": "Format All",
            "dependsOn": ["Format Proto", "Format Server", "Format App"]
        }
    ]
}